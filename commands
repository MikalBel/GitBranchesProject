mkdir Branches -p GitbranchesProject
git init
open folder
mkdir config src 
touch config/app.config
touch settings.json 
touch .gitignore 
Ignore all content under config, ignore settings.json file, ignore files with .log extension
touch src/index.html
add .
commit -m "First commit"
add code to index.html
add .
commit -m "add content to index page"
git branch feature/login
touch src/login.html
add src/login.html
commit -m "add login page"
add code to login 
git add .
commit -m "add contetnt to login"
git checkout master 
git branch -b feature/signup
touch src/signup.html
add .
commit -m "add signup page" 
uppdate signup.html
add .
commit -m "add content signup page"
git merge feature/login
git checkout feature/signup
git rebase master
git checkout master
git merge feature/signup
creat new GitBranchesProject repo
copy GitBranchesProject repo from remote to local repo
git rename branch -M main
git push origin -u main
add readme text and commit in GitHub
git pull

